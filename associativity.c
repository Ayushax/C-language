#include <stdio.h>

int main(){
    int a = 3;
    int b = 6;
    int c = 9;
    printf("The value is: %d\n",a*b/c);
    printf("The value is: %d",a*b/c + 7);
    printf("The value is: %d",3*b/2*c + 7*a);
    // for */% left to right 
    // then +- come then =
    //  use () for more easy ways and understanding.
     return 0;
}